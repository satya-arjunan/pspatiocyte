Each subdirectory with the name of a simulator contains a run.py python executable file that will record the simulator run time and output for the benchmark Michaelis-Menten model. After executing all the simulators, you can view the reaction output by:

a. python plot_output.py

To plot the run times of all the simulators:

a. python plot_benchmark.py
